# conda env for the 0D sensitivity analysis (sensitivity_analysis/).

name: erpm-sa
channels:
  - conda-forge
dependencies:
  - python=3.12
  - numpy>=2.0
  - scipy>=1.13
  - sympy>=1.13
  - salib>=1.5
  - joblib>=1.3
  - matplotlib>=3.8   # only for the result plots; runs fine headless (Agg)
